apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
data:
  APP_ENV: production
  DB_HOST: prod-db.internal
  DB_USER: admin
  DB_PASSWORD: "prod-db-pass-123"
  REDIS_PASSWORD: "redis-secret-abc"
  API_KEY: "hardcoded-api-key-xyz789"
